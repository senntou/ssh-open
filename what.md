# 背景
リモートに ssh して作業をしているとき、画像の閲覧をしたいタイミングは度々ある。
その時、別タブでVSCodeを開くとか、そういうことはしたくない。
ssh先でコマンドを打って、それによりローカルでGUIが開かれるのが最も理想的だ。

# 作りたいもの
- SSH でリモートに接続し、リバーストンネルを繋いでおく
- リモートからコマンドを打つ
    - `ssh-open .` みたいな
- トンネルを通じて、ローカルのhttpサーバーにリクエストを送る
    - その際、pathの情報をリクエストに含める
- ローカルで起動済みだったサーバーがリクエストを受け取り、GUIを起動する。


ローカルでのサーバーの起動は手動で行うものとする。慣れてきたらsystemdで自動起動するかもだが。
GUIの種類については、考える必要あり。
本当はexplorerなどを利用することを考えていたが、これは sshfs などの仕組みが必要になる。
これは良いかどうか正直分からない。

sshfs を使わないでやろうとすると、手段はかなり限られる。
私が思いつくのは、remoteでhttp サーバーを起動し、そこにアクセスするGUIを自作することだ。


